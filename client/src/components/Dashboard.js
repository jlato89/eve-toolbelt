import React, { Component } from 'react';

export class Dashboard extends Component {
   componentDidMount() {
      // grab auth data from db
   }

   render() { 
      return (
         <div>
            <h1>Dashboard Page</h1>
         </div>
      );
   }
}

export default Dashboard;
